doctrine:
  dbal:
    default_connection: data
    connections:
      data:
        url: ~
        driver: pdo_sqlite
        path: '%kernel.project_dir%/var/payment.test.data.sqlite'
        server_version: '3.45.1'
      infra:
        url: ~
        driver: pdo_sqlite
        path: '%kernel.project_dir%/var/payment.test.infra.sqlite'
        server_version: '3.45.1'
